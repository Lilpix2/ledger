[project]
name = "ledger"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "textual>=1.0",
]

[project.scripts]
ledger = "ledger.tui_app:main"

[tool.setuptools.packages.find]
where = ["src"]
